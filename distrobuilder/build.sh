#!/usr/bin/env bash

# sudo mkdir -p /usr/share/debootstrap/scripts
# sudo distrobuilder build-incus --vm kali.yaml -o image.architecture=amd64 -o image.variant=cloud -o image.release=kali-rolling
# sudo distrobuilder build-incus --vm kali.yaml -o image.architecture=amd64 -o image.variant=gnome -o image.release=kali-rolling
# sudo distrobuilder build-incus --vm archlinux.yaml -o image.architecture=amd64 -o image.variant=gnome